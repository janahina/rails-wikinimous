
<div class="navbar flexbox">
<%= link_to("Wikilies", articles_path) %>
<%= link_to "New article", new_article_path %>
</div>
<div class="container">
  <div>
    <% @articles.each do |article| %>
      <h2><%= link_to article.title, article_path(article) %></h2>
      <p><%= article.content %></p>
    <% end %>
  <div>
</div>
